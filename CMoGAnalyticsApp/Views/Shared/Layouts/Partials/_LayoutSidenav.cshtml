@{bool isHorizontal = ViewData["LayoutSidenavHorizontal"] != null && ViewData["LayoutSidenavHorizontal"].ToString() == "True";}
@{string currentPage = ViewContext.RouteData.Values["Controller"].ToString() + "/" + ViewContext.RouteData.Values["Action"].ToString();}

<!-- Layout sidenav -->
<div id="layout-sidenav" class="@(isHorizontal ? "layout-sidenav-horizontal sidenav-horizontal container-p-x flex-grow-0" : "layout-sidenav sidenav-vertical") sidenav bg-sidenav-theme">
    <!-- Inner -->
    <ul class="sidenav-inner@(!isHorizontal ? " py-1" : "")">
        <li class="sidenav-item@(currentPage == "Home/Index" ? " active" : "")">
            <a href="@Url.Action("Index", "Home")" class="sidenav-link"><i class="sidenav-icon ion ion-ios-home"></i><div>Home</div></a>
        </li>
        <li class="sidenav-item@(currentPage == "Heatmap/Index" ? " active" : "")">
            <a href="@Url.Action("Index", "Heatmap")" class="sidenav-link"><i class="sidenav-icon ion ion-md-map"></i><div>Heatmap</div></a>
        </li>
        <li class="sidenav-item@(currentPage == "Analytics/Index" ? " active" : "")">
            <a href="@Url.Action("Index", "Analytics")" class="sidenav-link"><i class="sidenav-icon ion ion-md-analytics"></i><div>Analytics</div></a>
        </li>
        <li class="sidenav-item@(currentPage == "Profile/Index" ? " active" : "")">
            <a href="@Url.Action("Index", "Profile")" class="sidenav-link"><i class="sidenav-icon ion ion-md-contact"></i><div>Profile</div></a>
        </li>
    </ul>
</div>
<!-- / Layout sidenav -->
